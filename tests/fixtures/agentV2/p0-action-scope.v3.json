{
  "schemaVersion": 3,
  "policyVersion": "agent-p0-action-scope-v3",
  "cases": [
    {
      "id": "p0.direct-swap-prepare", "inputKind": "direct", "currentMessage": "Swap 10 TON to USDT",
      "expected": { "scenario": "prepare-swap", "responseMode": "swap_prepare_allowed", "permittedPrepareTools": ["action.swap.prepare"], "permittedActions": ["swap"], "providerCallCountBeforeDecision": 1, "walletToolCallCountBeforeDecision": 0 }
    },
    {
      "id": "p0.direct-swap-buy-destination", "inputKind": "direct", "currentMessage": "Buy 10 TON with USDT",
      "expected": { "scenario": "prepare-swap", "responseMode": "swap_prepare_allowed", "permittedPrepareTools": ["action.swap.prepare"], "permittedActions": ["swap"], "providerCallCountBeforeDecision": 1, "walletToolCallCountBeforeDecision": 0 }
    },
    {
      "id": "p0.direct-swap-buy-source", "inputKind": "direct", "currentMessage": "Buy TON for 10 USDT",
      "expected": { "scenario": "prepare-swap", "responseMode": "swap_prepare_allowed", "permittedPrepareTools": ["action.swap.prepare"], "permittedActions": ["swap"], "providerCallCountBeforeDecision": 1, "walletToolCallCountBeforeDecision": 0 }
    },
    {
      "id": "p0.direct-swap-prepare-ru", "inputKind": "direct", "currentMessage": "Обменяй 10 TON на USDT",
      "expected": { "scenario": "prepare-swap", "responseMode": "swap_prepare_allowed", "permittedPrepareTools": ["action.swap.prepare"], "permittedActions": ["swap"], "providerCallCountBeforeDecision": 1, "walletToolCallCountBeforeDecision": 0 }
    },
    {
      "id": "p0.direct-swap-informational", "inputKind": "direct", "currentMessage": "How do swaps work?",
      "expected": { "scenario": "general", "responseMode": "informational_only", "permittedPrepareTools": [], "permittedActions": [], "providerCallCountBeforeDecision": 0, "walletToolCallCountBeforeDecision": 0 }
    },
    {
      "id": "p0.direct-stake-open", "inputKind": "direct", "currentMessage": "Stake 10 TON",
      "expected": { "scenario": "open-staking", "responseMode": "stake_action_allowed", "permittedPrepareTools": [], "permittedActions": ["stake"], "providerCallCountBeforeDecision": 1, "walletToolCallCountBeforeDecision": 0 }
    },
    {
      "id": "p0.direct-diesel-informational", "inputKind": "direct", "currentMessage": "Check whether Diesel can pay this fee",
      "expected": { "scenario": "general", "responseMode": "informational_only", "permittedPrepareTools": [], "permittedActions": [], "providerCallCountBeforeDecision": 0, "walletToolCallCountBeforeDecision": 0 }
    },
    {
      "id": "p0.portfolio-check-swap", "inputKind": "portfolio_followup", "currentMessage": "Explain swap options for this holding", "followupKind": "check_swap", "followupIntent": "informational",
      "expected": { "scenario": "portfolio-analysis", "responseMode": "informational_only", "permittedPrepareTools": [], "permittedActions": [], "providerCallCountBeforeDecision": 0, "walletToolCallCountBeforeDecision": 0 }
    },
    {
      "id": "p0.portfolio-check-staking", "inputKind": "portfolio_followup", "currentMessage": "Explain staking options for this holding", "followupKind": "check_staking", "followupIntent": "informational",
      "expected": { "scenario": "portfolio-analysis", "responseMode": "informational_only", "permittedPrepareTools": [], "permittedActions": [], "providerCallCountBeforeDecision": 0, "walletToolCallCountBeforeDecision": 0 }
    },
    {
      "id": "p0.direct-send-prepare", "inputKind": "direct", "currentMessage": "Send 10 TON to Mom",
      "expected": { "scenario": "prepare-send", "responseMode": "send_prepare_allowed", "permittedPrepareTools": ["action.send.prepare"], "permittedActions": [], "providerCallCountBeforeDecision": 0, "walletToolCallCountBeforeDecision": 0 }
    },
    {
      "id": "p0.direct-receive-prepare", "inputKind": "direct", "currentMessage": "Open my Receive screen",
      "expected": { "scenario": "prepare-receive", "responseMode": "receive_action_allowed", "permittedPrepareTools": [], "permittedActions": ["receive"], "providerCallCountBeforeDecision": 0, "walletToolCallCountBeforeDecision": 0 }
    }
  ]
}
